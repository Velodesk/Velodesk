/**
 * test-workflow-finalizados-filter — filtros da fila Finalizados
 * Rode (backend): npx tsx scripts/test-workflow-finalizados-filter.ts
 * Nota: import direto do frontend exige bundler Vite; use o script espelho no backend.
 */
console.log('Use: cd backend && npx tsx scripts/test-workflow-finalizados-filter.ts');
